<h1 class="text-center mt-4 helvetica-neue font-dark-gray"><%= @user.username %></h1>
<% if logged_in? && @user == current_user %>
  <div class="text-center mt-4">
    <%= link_to "Edit your profile", edit_user_path(@user), class: "btn btn-outline-dark" %>
  </div>
<% end %>
<h3 class="text-center mt-4 helvetica-neue">article list</h3>
<div class="flickr_pagination">
  <%= will_paginate @articles, :container => false %>
</div>
<%= render 'articles/articles' %>
<div class="flickr_pagination">
  <%= will_paginate @articles, :container => false %>
</div>